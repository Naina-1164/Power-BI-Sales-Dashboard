# Version 3 - Basic DAX Measures

Create these measures in Power BI Desktop:

```DAX
Total Sales = SUM(sales_data[Sales])
Total Quantity = SUM(sales_data[Quantity])
```

Use `Total Sales` in the Total Sales card and `Total Quantity` in the Quantity card. Test the Category and Product slicers to see the measures respond to filters.
